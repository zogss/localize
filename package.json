{
  "name": "localize",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "yarn workspace @localize/server",
    "mobile": "yarn workspace @localize/mobile"
  },
  "devDependencies": {
    "lerna": "^8.0.1"
  },
  "packageManager": "yarn@4.6.0"
}
