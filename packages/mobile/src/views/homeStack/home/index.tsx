import { AppScreen } from '@app/components';
import React from 'react';

const HomeScreen: React.FC = () => {
  //* hooks

  //* render
  return <AppScreen withEdges={['top']}></AppScreen>;
};

export default HomeScreen;
